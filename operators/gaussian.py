from PIL.ImageFilter import Gaussian
from op import Op
from cst import *



class Gaussian(Op):
	def __init__(self, img, radius = 2.)
		super(Gaussian, self).__init__(voronoiPureRandom, N = N, thick = 1., seed = None)